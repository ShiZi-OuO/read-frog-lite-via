# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 记录发布变更。

## v1.2.1

- 改进脚本启动时机与悬浮球挂载：页面加载早期即可显示，避免等待页面完全就绪
- 兼容页面隐藏通用容器、延迟创建正文或替换根节点的情况，确保悬浮球保持可见且不会重复创建
- 正文就绪前点击翻译会保留一次操作意图，正文出现后再启动；自动翻译仍按原设置执行
- 增加启动流程回归测试，覆盖延迟 DOM、根节点替换、重复挂载与隐藏容器
- 修复点击悬浮球后扫描整页造成的短暂停顿：先显示加载状态，并减少扫描中的重复布局读取
- 兼容严格内容安全策略（CSP）：使用构造样式表加载悬浮界面和译文样式，避免内联样式被网站拦截

## v1.2.0

- 修复用户输入保护：`contenteditable` 的空值、裸属性、`plaintext-only`、继承值与 `designMode` 全部纳入判定，输入框和可编辑区里的草稿不会再被读取、发送或就地改写
- 页面内容被原地改写后（框架重渲染、虚拟列表复用、AI 逐字输出、相对时间刷新）会重新翻译，不再显示与当前原文不符的旧译文
- 持续高速变化的页面不再中断翻译：观察器改为“防抖 + 最大等待”，首轮之后也会继续跟进新增内容
- 展开 `<details>`、切换 `hidden`、改写 `placeholder`、`value`、`title`、`alt` 等属性后会自动补翻译
- 取消 500 段硬上限：长文（1000+ 段落、上千行表格）能一直翻译到页面底部
- 超过 5000 字符的单段按句子切分翻译后无损拼回，不再整段跳过，并保护代理对、组合记号与 emoji
- 消除容器与其内部链接被重复翻译、重复计费的问题：同一批次内相同原文只请求一次，并发中的相同文本也会合并
- 语言判定改为白名单：希伯来文、希腊文、天城文等不再被误判为“已是目标语言”而整站零翻译
- 请求前跳过 URL、邮箱、十六进制与寄存器地址、版本号、纯数字与金额，减少无效消耗
- 限流与服务器错误改为指数退避重试（最多 3 次）并优先尊重 `Retry-After`；点击停止翻译会立即中止重试
- 界面文字过滤收敛：工具栏、菜单、标签栏只排除容器自身，其中的导航、页脚与按钮文字仍会被翻译
- `[role=button]`、`dialog` 等不再整块屏蔽内部可读文字
- 视口外与 1px 裁剪的隐藏文本不再消耗翻译额度；广告位按保守标记跳过
- 长页面渲染改进：分批显示时只排序一次，长文翻译过程中滚动更顺畅
- 动态页面的增量扫描范围收敛到真正变化的区域，减少重复扫描与无效布局查询

## v1.1.1

- 翻译前保守跳过已经是目标语言的纯文本与纯数字内容，减少中文界面的无效 AI 请求
- 改进混合语言翻译提示，保留已有目标语言内容，只翻译其余部分
- AI 批量响应格式异常时使用受控双通道补救，缩短逐段串行等待
- 增加容量受限的跨页面本地翻译缓存，且缓存键不包含 API Key
- 保留批量请求与并发效率，同时将返回结果按阅读优先级逐条显示，减少整批译文突然跳出的观感

## v1.1.0

- 支持按网站记忆自动翻译偏好，并保持默认手动触发
- 移除不适合 Via 手机阅读场景的划词翻译、复制译文与系统朗读工具
- 移植 Read Frog 上游基于实际页面布局的正文遍历思路，减少对固定标签和站点结构的依赖
- 改进标题、署名、发布时间、嵌套行内文字和非语义化正文区域的识别
- 直接替换模式改为安全回填文字节点，避免清除卡片中的图片、链接、图标和其他嵌套结构
- 修复部分 Chromium 与 Android WebView 中行内元素 `innerText` 混入同一行兄弟文字的问题

## v1.0.1

- 支持使用连续 `<br>` 而非段落标签排版的旧式新闻正文，并确保恢复原文后不残留临时结构
- 修复翻译期间悬浮球半隐藏时加载圈被屏幕边缘遮住的问题
- 支持翻译导航、新闻链接、按钮、页脚等可交互文字，并保留原有跳转、事件和图标
- 支持动态加载后新增的可交互内容，避免重复扫描和重复翻译
- 防止直接替换模式移除新闻卡片中的图片、视频、图标及其他媒体组件
- 补充识别正文之外的文章标题、摘要、作者和发布时间区域
- 改进多栏首页与多 `<article>` 信息流识别，覆盖普通卡片中的简介、作者和短元数据
- 改进临时文字包装与局部重试清理，确保恢复原文时完整还原页面结构
- 长页面候选按可见区域与真实阅读顺序排列，避免可交互链接被数量上限挤出翻译队列
- 恢复译文链接在鼠标悬停和键盘聚焦时的下划线反馈，同时保留链接颜色与点击行为

## v1.0.0

- 完成 Via 浏览器与 Android WebView 的移动端适配
- 基于 Read Frog 思路进行二次开发，发布独立 Via 用户脚本
- 支持 Microsoft、OpenAI、DeepSeek 与 OpenAI Compatible AI 翻译接口
- 支持渐进式双语翻译、直接替换原文与原文恢复
- 支持划词翻译、复制译文与系统朗读
- 提供 Material 风格设置界面、可贴边隐藏的青蛙悬浮球
- 提供翻译缓存、批量与并发控制、局部失败重试和正文智能识别
